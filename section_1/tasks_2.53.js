// what is the outpot of the script? (çıktı olarak ne görürsünüz?)

let name = 'ELA';

alert(`hello ${1}`)  // hello1

alert(`hello ${"name"}`);   // hello name

alert(`hello ${name}`);  // hello ELA
