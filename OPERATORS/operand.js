// // operand oporatörlerini uygulandığı ( +,-,* vs.) değerlerdir.
// Örneğin çarpma işleme için 5*2 Örneğinden gidersek. 
// İki tane operand vardır. Bunlardan saolda olan 5 ve sağ operand 2. Bunlara argüman da denir

///**  eğer tek operand(argüman) dan oluşurda bu operatör (unary) olarak adlandırılır.
// örn; "-" sayının işaratini değiştirir.

let x = 1 ;
x = -x ;
alert(x);   // -1 unary işlemi gerçekleşti.

// *** eğer operatörün iki tane operand ı var ise (binary operand) denir.

let y = 1, z = 3 ;

alert(z - y); // 2, iki sayını çıkarılması binary operand işlemidir.

// şeklen iki operatörden konuşuruz unary çıkarma [tek operand işareti değiştirir.]
// binary çıkarma (iki operatör çıkarma)

